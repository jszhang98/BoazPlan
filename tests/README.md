# Tests

- Run unit tests: `npm test` or `npm run test:unit`
- E2E tests available in `tests/e2e/` (Playwright). See `tests/e2e/README.md` for instructions.
- To scaffold additional test architecture or integrate with CI, run `/bmad_tea_framework`.
