# BoazPlan API

Simple Fastify-based backend for the BoazPlan project. 
Routes are under `/api/v1/`.

Run locally:

```bash
cd apps/api
npm install
npm run dev
```

The server listens on port 4000 by default.
